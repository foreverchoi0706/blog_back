{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "uriai_back",
      "preLaunchTask": "npm: build",
      "program": "${workspaceFolder}/src/app.ts",
      "outFiles": ["${workspaceFolder}/build/**/*.js"]
    }
  ]
}
